# Footbot Python dependencies
# Requires Python 3.10.x exactly (Rasa 3.6.x does NOT support 3.11+)
# Recommended: pyenv install 3.10.14 && pyenv local 3.10.14

# ── Rasa NLU + dialogue framework ────────────────────────────────────────────
rasa==3.6.21

# ── TensorFlow — macOS arm64 (Apple Silicon) ─────────────────────────────────
# Replace these two lines with tensorflow==2.11.0 on x86_64 / Linux
tensorflow-macos==2.11.0
tensorflow-metal==0.7.1

# ── Pipeline: scraping & data storage ────────────────────────────────────────
requests==2.28.2
beautifulsoup4==4.12.2
lxml==4.9.3
pandas==2.0.3
pyarrow==14.0.1
# FBref is behind Cloudflare — undetected-chromedriver uses real Chrome to bypass it
# Requires Google Chrome installed at /Applications/Google Chrome.app (macOS)
undetected-chromedriver==3.5.5
selenium==4.41.0

# ── Prediction engine ─────────────────────────────────────────────────────────
numpy==1.24.3
scipy==1.11.4

# ── Rasa runtime transitive dep (keep pinned) ─────────────────────────────────
aiohttp==3.8.6

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==7.4.4
